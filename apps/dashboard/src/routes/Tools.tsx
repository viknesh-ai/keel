import { Placeholder } from "./Placeholder.tsx";

export default function ToolsRoute() {
  return (
    <Placeholder
      title="Tools"
      body="The tool catalogue, versions and per-environment bindings. Built in slice 1."
    />
  );
}
